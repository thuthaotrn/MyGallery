import { LikedClassPipe } from './liked-class.pipe';

describe('LikedClassPipe', () => {
  it('create an instance', () => {
    const pipe = new LikedClassPipe();
    expect(pipe).toBeTruthy();
  });
});
